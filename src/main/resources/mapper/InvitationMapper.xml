<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.logistics.track17.mapper.InvitationMapper">

    <resultMap id="InvitationResultMap" type="com.logistics.track17.entity.Invitation">
        <id property="id" column="id"/>
        <result property="companyId" column="company_id"/>
        <result property="shopId" column="shop_id"/>
        <result property="email" column="email"/>
        <result property="roleId" column="role_id"/>
        <result property="token" column="token"/>
        <result property="status" column="status"/>
        <result property="expiresAt" column="expires_at"/>
        <result property="invitedBy" column="invited_by"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.logistics.track17.entity.Invitation" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO invitations (company_id, shop_id, email, role_id, token, status, expires_at, invited_by, created_at, updated_at)
        VALUES (#{companyId}, #{shopId}, #{email}, #{roleId}, #{token}, #{status}, #{expiresAt}, #{invitedBy}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.logistics.track17.entity.Invitation">
        UPDATE invitations
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="roleId != null">role_id = #{roleId},</if>
            updated_at = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="InvitationResultMap">
        SELECT * FROM invitations WHERE id = #{id}
    </select>

    <select id="selectByToken" resultMap="InvitationResultMap">
        SELECT * FROM invitations WHERE token = #{token}
    </select>

    <select id="selectByCompanyId" resultMap="InvitationResultMap">
        SELECT * FROM invitations WHERE company_id = #{companyId}
        ORDER BY created_at DESC
    </select>

    <select id="selectByEmail" resultMap="InvitationResultMap">
        SELECT * FROM invitations WHERE email = #{email}
        ORDER BY created_at DESC
    </select>

    <select id="selectPendingByEmail" resultMap="InvitationResultMap">
        SELECT * FROM invitations
        WHERE email = #{email} AND status = 'PENDING' AND (expires_at IS NULL OR expires_at > NOW())
        ORDER BY created_at DESC
    </select>

    <update id="updateStatus">
        UPDATE invitations SET status = #{status}, updated_at = NOW() WHERE id = #{id}
    </update>

</mapper>
