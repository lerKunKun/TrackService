<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.logistics.track17.mapper.DingtalkSyncLogMapper">

    <!-- ResultMap -->
    <resultMap id="BaseResultMap" type="com.logistics.track17.entity.DingtalkSyncLog">
        <id column="id" property="id"/>
        <result column="sync_type" property="syncType"/>
        <result column="sync_mode" property="syncMode"/>
        <result column="status" property="status"/>
        <result column="total_count" property="totalCount"/>
        <result column="success_count" property="successCount"/>
        <result column="failed_count" property="failedCount"/>
        <result column="error_message" property="errorMessage"/>
        <result column="started_at" property="startedAt"/>
        <result column="completed_at" property="completedAt"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <!-- 查询所有同步日志 -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM dingtalk_sync_logs
        ORDER BY id DESC
    </select>

    <!-- 查询最近N条同步日志 -->
    <select id="selectRecent" resultMap="BaseResultMap">
        SELECT * FROM dingtalk_sync_logs
        ORDER BY id DESC
        LIMIT #{limit}
    </select>

    <!-- 根据ID查询 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM dingtalk_sync_logs
        WHERE id = #{id}
    </select>

    <!-- 根据同步类型查询 -->
    <select id="selectBySyncType" resultMap="BaseResultMap">
        SELECT * FROM dingtalk_sync_logs
        WHERE sync_type = #{syncType}
        ORDER BY id DESC
    </select>

    <!-- 插入同步日志 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO dingtalk_sync_logs (
            sync_type,
            sync_mode,
            status,
            total_count,
            success_count,
            failed_count,
            error_message,
            started_at,
            completed_at
        ) VALUES (
            #{syncType},
            #{syncMode},
            #{status},
            #{totalCount},
            #{successCount},
            #{failedCount},
            #{errorMessage},
            #{startedAt},
            #{completedAt}
        )
    </insert>

    <!-- 更新同步日志 -->
    <update id="update">
        UPDATE dingtalk_sync_logs
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="totalCount != null">total_count = #{totalCount},</if>
            <if test="successCount != null">success_count = #{successCount},</if>
            <if test="failedCount != null">failed_count = #{failedCount},</if>
            <if test="errorMessage != null">error_message = #{errorMessage},</if>
            <if test="completedAt != null">completed_at = #{completedAt},</if>
        </set>
        WHERE id = #{id}
    </update>

</mapper>
