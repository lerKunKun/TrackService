<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.logistics.track17.mapper.ParcelMapper">

    <resultMap id="BaseResultMap" type="com.logistics.track17.entity.Parcel">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="parcel_no" property="parcelNo"/>
        <result column="carrier_code" property="carrierCode"/>
        <result column="carrier_name" property="carrierName"/>
        <result column="shipped_at" property="shippedAt"/>
        <result column="delivered_at" property="deliveredAt"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert" parameterType="com.logistics.track17.entity.Parcel" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO parcels (order_id, parcel_no, carrier_code, carrier_name, shipped_at, delivered_at, status)
        VALUES (#{orderId}, #{parcelNo}, #{carrierCode}, #{carrierName}, #{shippedAt}, #{deliveredAt}, #{status})
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM parcels WHERE id = #{id}
    </select>

    <select id="selectByOrderId" resultMap="BaseResultMap">
        SELECT * FROM parcels WHERE order_id = #{orderId} LIMIT 1
    </select>

</mapper>
